const { it } = require("node:test");

describe("First suite" , () => {
    it("First test" , () => {
        cy.visit("/");
        

    })

it("First" , () => {
    cy.get(<span class="menu-title">Layout</span>)

})
})